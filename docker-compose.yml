version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./iching:/app/iching
    environment:
      - FLASK_APP=iching/src/app.py
      - FLASK_ENV=production
    restart: unless-stopped 